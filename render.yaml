services:
  # ------------------------------------
  # 1. API Server (Express)
  # ------------------------------------
  - type: web
    name: ai-triage-server
    env: node
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm start
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false # User must provide this
      - key: REDIS_HOST
        fromService:
          type: redis
          name: ai-triage-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: ai-triage-redis
          property: port
      - key: OPENAI_API_KEY
        sync: false
    autoDeploy: false

  # ------------------------------------
  # 2. Background Worker (BullMQ)
  # ------------------------------------
  - type: worker
    name: ai-triage-worker
    env: node
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm run start:worker
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: REDIS_HOST
        fromService:
          type: redis
          name: ai-triage-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: ai-triage-redis
          property: port
      - key: OPENAI_API_KEY
        sync: false
    autoDeploy: false

  # ------------------------------------
  # 3. Client (Next.js)
  # ------------------------------------
  - type: web
    name: ai-triage-client
    env: node
    plan: free
    buildCommand: cd client && npm install && npm run build
    startCommand: cd client && npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: ai-triage-server
          property: url
    autoDeploy: false

  # ------------------------------------
  # 4. Redis (Private Service)
  # ------------------------------------
  - type: redis
    name: ai-triage-redis
    ipAllowList: [] # Only internal access
    plan: free # Render Free tier Redis instance
